 # Intro


 ## Rush
 Rush are the week end and need to be send before 23:42

 ## Set Up Git

 # Evaluation
 ## Before (evaluated student)
 1) Set the project as finished on intra.42.fr 
 2) Retrieve Git Clone link from intra page
 3) Verify that what you push is correct (No file / Commit Missing)
 4) Check for an evaluation slot
 5) Go to the student place and log to a computer next to him , if not possible log on his computer
 
 ## Before (evaluator)
 1) Go to profile page > Pending Evaluations > Manage Slots
 2) Create a slot and wait for the notification that it as been taken
 3) Ensure you know the subject or read about it
 4) 15 before the evaluation we receive mail of the person that we will evaluate
 5) Go to their profile and check if they have already been evaluated on that project **if so read the comment carrefully**

## During the evaluation
1) Go to Intra > Click Begin Evaluation
2) Clone Repo
3) Check if all needed files are here > If not click "Empty Work"
4) Run Norminette > if the Norminette has error > Continue Interview and set the evaluation as "Norm Error"

# Exam 

- Register to Exam **Project**
- Register to Exam **Event Calendar**

- Mute Sound

## Important to remember

### Testing
```bash
alias cc42='cc -g -Wall -Wextra -Werror'
alias normi42='norminette -R CheckForbiddenSourceHeader'
```
### Man

```bash
man -k <search pattern>
man 2 write
man 3 write
```

### VScode
- Locate Editor: Tab Size and change the value to 4.
- (Optional) Check the box for Editor: Insert Spaces

### Vim
- https://devhints.io/vim
- https://github.com/LexouDuck/42/blob/master/.vimrc


```bash
:syntax on
:set number
:set autointdent
:set tabstop=4 :set ts=4
set shiftwidth=4
```

`yy`- yank (copy) a line
`2yy` - yank (copy) 2 lines
`yw` - yank (copy) the characters of the word from the cursor position to the start of the next word

`p` - put (paste) the clipboard after cursor
`P`- put (paste) before cursor


`dd` - delete (cut) a line
`2dd` - delete (cut) 2 lines

`:g/{pattern}/d` - delete all lines containing pattern
`:g!/{pattern}/d` - delete all lines not containing pattern

`d$` or `D `- delete (cut) to the end of the line
`x` - delete (cut) character


`:wq` or `:x` or `ZZ` - write (save) and quit